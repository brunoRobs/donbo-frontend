import { toast } from "vue3-toastify";

const settings = {
    multiple: false,
    clearOnUrlChange: false,
    position: "top-right",
    autoClose: 5000,
    closeButton: false,
    transition: "slide",
    pauseOnHover: true,
    pauseOnFocusLoss: true,
    closeOnClick: true,
    theme: "light"
}

export { toast, settings }